// Meteor.methods({
//     members: function(args) {
//         return {
//             rows:Meteor.users.find(args.selector,args.projection).fetch(),
//             total:Meteor.users.find(args.selector,args.projection).count()
//         };
//     },
// });
